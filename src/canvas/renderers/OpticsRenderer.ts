// Stub: Optics rendering — implemented in Step 4
import type { LightSource, OpticsObject, ShadowResult, Theme } from '../../types';
import type { ICoordinateSystem } from '../CoordinateSystem';

export function renderOptics(
  _ctx: CanvasRenderingContext2D,
  _opticsObjects: OpticsObject[],
  _lightSources: LightSource[],
  _shadowResults: ShadowResult[],
  _theme: Theme,
  _coords: ICoordinateSystem,
): void {
  // No-op until Step 4
}
