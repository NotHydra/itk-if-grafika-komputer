// Stub: Line rendering — implemented in Step 2
import type { LineSegment, Theme } from '../../types';
import type { ICoordinateSystem } from '../CoordinateSystem';

export function renderLines(
  _ctx: CanvasRenderingContext2D,
  _lines: LineSegment[],
  _theme: Theme,
  _coords: ICoordinateSystem,
): void {
  // No-op until Step 2
}
