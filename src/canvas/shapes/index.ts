// Import each shape file to trigger its self-registration side effect.
// To add a new shape: create a *.shape.ts file and add ONE import line here. That's it.
import './arrow.shape';
import './pin.shape';
import './pencil.shape';

